# pnpm 11+ reads project settings from this file, not package.json#pnpm or .npmrc.
nodeLinker: hoisted
shamefullyHoist: true

overrides:
  ioredis: 5.10.1

# Only packages that currently need a native install script on Linux.
allowBuilds:
  "@parcel/watcher": true
  "@swc/core": true
